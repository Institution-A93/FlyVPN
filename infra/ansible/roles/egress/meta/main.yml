---
galaxy_info:
  role_name: egress
  description: Foreign egress — Xray VLESS-Reality server + unbound (рекурсивный DNS)
  license: AGPL-3.0-only
  min_ansible_version: "2.14"
  platforms:
    - name: Debian
      versions:
        - bookworm
dependencies: []
