---
# Точка входа. Прогон по ролям узлов; группы — из inventory.
- name: Configure egress nodes
  hosts: egress
  become: true
  roles:
    - egress

- name: Configure control plane
  hosts: control
  become: true
  roles:
    - control-plane
